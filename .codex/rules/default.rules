# SPDX-License-Identifier: MIT
# Repository-local Codex guardrails. These rules supplement task authority and
# AGENTS.md; they do not grant permission for an external mutation.

prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Pushing mutates the remote repository and requires explicit task authority.",
    match = [
        "git push",
        "git push origin feature-branch",
        "git push --set-upstream origin feature-branch",
    ],
    not_match = [
        "git status",
        "git fetch origin",
    ],
)

prefix_rule(
    pattern = ["git", "tag", ["-s", "--sign", "-a", "--annotate", "-d", "--delete"]],
    decision = "prompt",
    justification = "Creating, signing, or deleting a tag requires explicit task authority.",
    match = [
        "git tag -s v9.9.9",
        "git tag --delete local-test-tag",
    ],
    not_match = [
        "git tag --list",
        "git show v9.9.9",
    ],
)

prefix_rule(
    pattern = ["gh", "pr", "merge"],
    decision = "prompt",
    justification = "Merging a pull request changes the protected delivery state and requires confirmation.",
    match = [
        "gh pr merge 56 --squash --delete-branch",
    ],
    not_match = [
        "gh pr view 56",
        "gh pr checks 56",
    ],
)

prefix_rule(
    pattern = ["gh", "release", ["create", "delete", "edit", "upload"]],
    decision = "prompt",
    justification = "Changing release assets or metadata requires explicit release authority.",
    match = [
        "gh release create v9.9.9",
        "gh release upload v9.9.9 package.rpm",
    ],
    not_match = [
        "gh release view v9.9.9",
        "gh release verify v9.9.9",
    ],
)

prefix_rule(
    pattern = ["gh", "workflow", "run"],
    decision = "prompt",
    justification = "Dispatching a workflow changes remote CI or release state and requires confirmation.",
    match = [
        "gh workflow run qualification.yml",
    ],
    not_match = [
        "gh workflow view qualification.yml",
        "gh run view 123456",
    ],
)

prefix_rule(
    pattern = ["gh", "run", ["rerun", "cancel", "delete"]],
    decision = "prompt",
    justification = "Changing an existing workflow run requires explicit task authority.",
    match = [
        "gh run rerun 123456",
        "gh run cancel 123456",
    ],
    not_match = [
        "gh run view 123456",
        "gh run watch 123456",
    ],
)

# Cover the ordinary flag positions Codex uses. The general git-push rule still
# prompts for every other push form; AGENTS.md remains authoritative if a
# non-canonical spelling is proposed.
prefix_rule(
    pattern = ["git", "push", ["--force", "-f", "--force-with-lease"], "origin", "main"],
    decision = "forbidden",
    justification = "Never force-push main; deliver through a reviewed pull request.",
    match = [
        "git push --force origin main",
        "git push --force-with-lease origin main",
    ],
    not_match = [
        "git push origin feature-branch",
    ],
)

prefix_rule(
    pattern = ["git", "push", "origin", ["--force", "-f", "--force-with-lease"], "main"],
    decision = "forbidden",
    justification = "Never force-push main; deliver through a reviewed pull request.",
    match = [
        "git push origin -f main",
    ],
    not_match = [
        "git push origin main",
    ],
)

prefix_rule(
    pattern = ["git", "push", "origin", "main", ["--force", "-f", "--force-with-lease"]],
    decision = "forbidden",
    justification = "Never force-push main; deliver through a reviewed pull request.",
    match = [
        "git push origin main --force",
    ],
    not_match = [
        "git push origin main",
    ],
)
